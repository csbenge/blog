<div class="container text-center">
  <div class="row">
    <div class="col col-3"></div>

    <div class="col col-6">

        <div class="card text-bg-secondary">
        <h3 class="card-header">Sign Up</h3>

        <% if not logged_in? %>

        <div class="card-body">
            <div class="container text-start">
            <p class="card-text">

            <div class="row">
              <div class="col">
                <%= form_for(@user) do |f| %>
                  <%= f.label :name, :class => "form-label" %>
                  <%= f.text_field :name, :class => "form-control" %>
                  <% @user.errors.full_messages_for(:name).each do |message| %>
                    <div class="bg-danger-subtle"><%= message %></div>
                  <% end %>

                  <br>
                  <%= f.label :email, :class => "form-label" %>
                  <%= f.email_field :email, :class => "form-control" %>
                  <% @user.errors.full_messages_for(:email).each do |message| %>
                    <div class="bg-danger-subtle"><%= message %></div>
                  <% end %>

                  <br>
                  <%= f.label :password, :class => "form-label" %>
                  <%= f.password_field :password, :class => "form-control" %>
                  <% @user.errors.full_messages_for(:password).each do |message| %>
                    <div class="bg-danger-subtle"><%= message %></div>
                  <% end %>

                  <br>
                  <%= f.label :password_confirmation, "Confirmation", :class => "form-label" %>
                  <%= f.password_field :password_confirmation, :class => "form-control" %>
                  <% @user.errors.full_messages_for(:password_confirmation).each do |message| %>
                    <div class="bg-danger-subtle"><%= message %></div>
                  <% end %>

                  <br>
                  <%= f.submit "Create Account", class: "btn btn-primary btn-sm" %>
                <% end %>
              </div>
            </div>
             
                
            </p>
            </div>
            </div>
        </div>

        <% else %>

        <div class="card-body text-center">
          <div class="container text-center">
            <p class="card-text text-center">
              <div class="row text-center">
              <div class="col text-center">
                <big>You already have an account and are logged in!</big>
              </div>
              </div>
            </p>
          </div>
        </div>

        <% end %>
    </div>
    <div class="col col-3"></div>
  </div>
</div>
