
<div class="container text-center">
  <div class="row">
    <div class="col col-3"></div>

    <div class="col col-6">

        <div class="card">
        <h3 class="card-header"><%= @user.name %></h3>
        <div class="card-body">
            <div class="container text-start">
            <p class="card-text">
                ID:      <%= @user.id %><br>
                Name:    <%= @user.name %><br>
                Email:   <%= @user.email %><br>
                Sign Up: <%= @user.created_at %>
            </p>
            </div>
            </div>
        </div>

    </div>
    <div class="col col-3"></div>
  </div>
</div>

